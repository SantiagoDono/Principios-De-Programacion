#include <iostream>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
int main()
{
    char letra;
    int A, E, I, O, U;
    cout << "ingrese una linea con minusculas, puntos y espacios, que tenga dos puntos seguidos:" << endl;
    cin >> letra;
    do{
        if(letra == '.')
            if (letra == ',') {
                cout << "bien";
            }

    } while (letra != '\n');
}

